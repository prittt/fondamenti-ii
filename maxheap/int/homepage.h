/**
@mainpage Max-Heap di `int`

In questa pagina trovi la documentazione per code di priorità (max-heap) di `int`. In particolare:

- \ref elemtype.h contiene la definizione di `ElemType` per il tipo `int`
e la documentazione delle funzioni a esso associate;
- \ref maxheap.h contiene la definizione del tipo `Heap` e la documentazione delle 
funzioni primitive (e non) relative alle code di priorità (max-heap). Si noti che 
il comportamento di queste funzioni è indipendente dalla definizione di `ElemType`.
*/