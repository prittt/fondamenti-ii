/**
@mainpage Liste Doppiamente Concatenate (Doubly Linked List) di `int`

In questa pagina trovi la documentazione per liste doppiamente concatenate di `int`. In particolare:

- \ref elemtype.h contiene la definizione di `ElemType` per il tipo `int`
e la documentazione delle funzioni a esso associate;
- \ref doublelist.h contiene la definizione del tipo `Item` e la documentazione delle 
funzioni primitive (e non) relative alle liste. Si noti che il comportamento di
queste funzioni è indipendente dalla definizione di `ElemType`.
*/