/**
@mainpage Liste di `char`

In questa pagina trovi la documentazione per liste di `char`. In particolare:

- \ref elemtype.h contiene la definizione di `ElemType` per il tipo `char`
e la documentazione delle funzioni a esso associate;
- \ref list.h contiene la definizione del tipo `Item` e la documentazione delle 
funzioni primitive (e non) relative alle liste. Si noti che il comportamento di
queste funzioni è indipendente dalla definizione di `ElemType`.
*/